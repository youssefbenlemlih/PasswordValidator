# PasswordValidator

A Programm that validates passwords to match specific rules:
  - contains no spaces
  - contains between 5 and 20 characters
  - has at least one number
  - has at least one special character
